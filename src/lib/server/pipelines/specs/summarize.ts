/**
 * Moved to @serene-pub/core-catalog (24 §9, T6): core's pipelines are a
 * publishable announcement now — SP is its first consumer, not its home.
 * This shim keeps the app's import paths stable; edit the catalog package.
 */
export * from "@serene-pub/core-catalog"
